{
  "buildCommand": "cd smart-room-twin/frontend && npm install && npm run build",
  "outputDirectory": "smart-room-twin/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
